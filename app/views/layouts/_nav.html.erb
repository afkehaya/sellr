<nav class="navbar navbar-default navbarpad">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_url %>">MM</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul id="parent-btn-become-merchant" class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li class="nav-new-listing">
        <% if current_user.merchant? %>
           <%= link_to new_listing_path do %>
              <span class="glyphicon glyphicon-plus new-listing-plus"></span> New Listing
            <% end %>
          <% end %>
        </li>
          <li><%= button_to "Become A Merchant", '/merchant', class: "btn btn-sm btn-warning btn-merchantapp" unless current_user.merchant? %></li>
          <li><%= link_to "Home", root_path, class: "navbar-brand" %></li>

          <li><%= link_to "My Account", edit_user_registration_path, class: "navbar-brand nav-link" %></li>
          <li><%= link_to "Log out", destroy_user_session_path, method: :delete, class: "navbar-brand" %></li>
      <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
