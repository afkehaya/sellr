<%= form_for(@listing) do |f| %>
  <% if @listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
      <% @listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :name %> <br>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :commission %> <br>
      <%= f.number_field :description, class: 'text_field', step: 'any' %>
    </div>

    <div class="field">
      <%= f.label :product_value %> <br>
      <%= f.number_field :value %>
    </div>

    <div class="field">
      <%= f.label :link_to_affiliate_marketing_campaign %> <br>
      <%= f.text_field :product_url %>
    </div>

    <div class="field">
      <%= f.label :description %> <br>
      <%= f.text_area :description %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
